model:
  d_model: 64
  nhead: 8
  num_layers: 4
  dropout: 0.1

data:
  max_digits: 3
  operators: ["+", "-"]
  batch_size: 256
  
training:
  lr: 0.002
  max_steps: 6000
  log_interval: 250
  save_interval: 1000
  device: "auto"
