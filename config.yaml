model:
  d_model: 32
  nhead: 4
  num_layers: 5
  dropout: 0.1

data:
  max_digits: 3
  operators: ["+", "-"]
  batch_size: 256
  
training:
  lr: 0.002
  max_steps: 10000
  log_interval: 250
  save_interval: 1000
  device: "auto"
