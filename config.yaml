model:
  d_model: 32
  nhead: 8
  num_layers: 2
  dropout: 0.0

data:
  max_digits: 3
  operators: ["+", "-"]
  batch_size: 256
  
training:
  lr: 0.01
  max_steps: 2400
  log_interval: 100
  save_interval: 400
  device: "auto"
