model:
  d_model: 64
  nhead: 4
  num_layers: 4
  dropout: 0.1

data:
  max_digits: 3
  operators: ["+", "-"]
  batch_size: 256
  
training:
  lr: 0.001
  max_steps: 2000
  log_interval: 250
  save_interval: 500
  device: "auto" # will use mps/cuda if available
